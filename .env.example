# Claude Code Crew 环境变量配置
# 复制此文件为 .env 并根据需要修改值

# ==================== 基本配置 ====================

# 应用暴露端口（默认 3001）
EXPOSED_PORT=3001

# ==================== 目录挂载配置 ====================

# 工作目录挂载路径（默认为当前目录 ${PWD}）
# 这是 Claude Code Crew 将要操作的工作空间目录
# WORKSPACE_DIR=/path/to/your/workspace

# Git 配置文件路径（默认为 ~/.gitconfig）
# 容器将使用此 Git 配置进行版本控制操作
# GIT_CONFIG_PATH=/path/to/your/.gitconfig

# SSH 密钥目录路径（默认为 ~/.ssh）
# 容器将使用此目录下的 SSH 密钥进行 Git 操作
# SSH_KEY_PATH=/path/to/your/.ssh

# ==================== 高级配置 ====================

# 工作目录（容器内路径，通常不需要修改）
# WORK_DIR=/workspace
